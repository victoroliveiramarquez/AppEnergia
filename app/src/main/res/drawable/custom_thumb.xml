<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Sombra para el thumb -->
    <item>
        <shape android:shape="oval">
            <solid android:color="#8BC34A" /> <!-- Color de sombra -->
            <size
                android:width="20dp"
                android:height="20dp" />
        </shape>
    </item>

    <!-- Círculo principal del thumb -->
    <item android:left="4dp" android:right="4dp" android:top="4dp" android:bottom="4dp">
        <shape android:shape="oval">
            <solid android:color="#8BC34A" /> <!-- Color del thumb -->
            <size
                android:width="20dp"
                android:height="20dp" />
        </shape>
    </item>

</layer-list>