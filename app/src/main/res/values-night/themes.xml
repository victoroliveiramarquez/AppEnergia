<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Tema de la aplicación para modo oscuro -->
    <style name="Base.Theme.AppEnergia" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:colorBackground">@color/black</item> <!-- Fondo oscuro -->
        <item name="colorPrimary">@color/primaryDarkColor</item>
        <item name="colorPrimaryVariant">@color/primaryDarkColor</item>
        <item name="colorSecondary">@color/secondaryColorDark</item>
        <item name="android:textColor">@color/white</item> <!-- Texto blanco -->
        <item name="android:colorAccent">@color/primaryDarkColor</item> <!-- Acento oscuro -->
    </style>
</resources>

